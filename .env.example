# URL công khai của site (dùng cho metadata, sitemap, canonical)
NEXT_PUBLIC_SITE_URL=https://monngonmoingay.vn

# Endpoint của Go backend (đọc dishes/recipes)
NEXT_PUBLIC_API_BASE=http://localhost:8080

# Secret để pipeline backend trigger ISR revalidate
REVALIDATE_SECRET=change-me
